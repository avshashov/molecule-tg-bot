name: build-and-deploy
on:
  push:
    branches:
      #  - main
      - 'feature/cicd'
  pull_request:
    branches:
      #  - main
      - 'feature/cicd'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push to Docker Hub
        uses: docker/build-push-action@v5
        with:
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_REPOSITORY }}/${{ secrets.DOCKERHUB_IMAGE }}:latest

  #  deploy:
  #    needs: [ build ]
  #    runs-on: ubuntu-latest
  #    steps:
  #      - name: Run deploy on server
  #        uses: appleboy/ssh-action@v1.0.3
  #        with:
  #          host: ${{ secrets.HOST }}
  #          username: ${{ secrets.USERNAME }}
  #          key: ${{ secrets.KEY }}
  #          port: ${{ secrets.PORT }}
  #          script: |
  #            cd ${{ DOCKER_COMPOSE_FILE_DIR }}
  #            docker compose down
  #            docker compose pull
  #            docker compose up -d

  notify:
    needs: [ build ]
    #    needs: [ deploy ]
    runs-on: ubuntu-latest
    steps:
      - name: Send telegram message on finished deploy
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          message: |
            *GitHub Action:* build-and-deploy
            *Pipeline:* https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
            *Jobs status:* ✅
            *Repository:* ${{ github.repository }}
            *Target branch:* ${{ github.ref_name }}
            *Event name:* ${{ github.event_name }}
            *Commit author:* ${{ github.actor }}

            *Commit message:* ${{ github.event.commits[0].message }}
